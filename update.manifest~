#定义规则
#版本号表明较大的变动，包括：1.三个服务器主文件之一变化 2.较多的html文件夹改动
#如果主服务器没有变化，则只启用次版本号，如8.1

版本 8：
	1.改进，全面使用jQuery，使得代码精简，工作量减少
	2.添加/echo.py	， 提供echo服务，所有post的数据都将原样传回
	3.在增加了io_util.js文件，其中增加了echo函数，该函数打印内容到 $('#__echo')上；任何包含了该函数的js页面都能使用echo函数。
